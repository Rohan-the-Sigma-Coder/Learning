def word_replacement():
    x = input('Enter a sentence: ')
    y = input('Enter a word or phrase in that sentence to replace: ')
    z = input('Enter a word or phrase to replace your original word: ')
    m = x.replace(y,z)
    print('Your new sentence is: ', m)

if d == 1:
    word_replacement()